/**
 * 1. let命令的特点
 * 答：
 * 1）不存在变量提升
 * 2）暂时性死区
 * 3）不允许重复声明（let不允许在相同作用域内，重复声明同一个变量）
 * 
 * 
 * 2. const命令的特点
 * 答：
 * 1）const声明一个只读的常量，旦声明，常量的值就不能改变。
 *  意味着，const一旦声明变量，就必须立即初始化。
 * 2）const的作用域与let命令相同，只在声明所在的块级作用内有效。
 * 3）不存在变量提升，同样存在暂时性死区，只能在声明后使用。
 */